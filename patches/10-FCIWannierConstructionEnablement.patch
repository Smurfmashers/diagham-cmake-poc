--- a/FQHE/src/HilbertSpace/FermionOnSphere.h
+++ b/FQHE/src/HilbertSpace/FermionOnSphere.h
@@ -258,6 +258,13 @@
   // return value = number of particles
   virtual int GetNbrParticles();
 
+  // get the underlying many-body state description (used by Wannier construction
+  // and other tools that need direct access to the Fock representation)
+  //
+  // index = state index in the Hilbert space
+  // return value = unsigned long bit-pattern representing the Fock state
+  virtual unsigned long GetStateDescription(int index);
+
   // get the particle statistic 
   //
   // return value = particle statistic
@@ -1345,6 +1352,17 @@
   return this->NbrFermions;
 }
 
+// get the underlying many-body state description (used by Wannier construction
+// and other tools that need direct access to the Fock representation)
+//
+// index = state index in the Hilbert space
+// return value = unsigned long bit-pattern representing the Fock state
+
+inline unsigned long FermionOnSphere::GetStateDescription(int index)
+{
+  return this->StateDescription[index];
+}
+
 // get the particle statistic 
 //
 // return value = particle statistic
--- a/FQHE/src/HilbertSpace/FermionOnTorusWithMagneticTranslations.h
+++ b/FQHE/src/HilbertSpace/FermionOnTorusWithMagneticTranslations.h
@@ -192,6 +192,26 @@
   // 
   // return avlue = maximum momentum along the y axis
   virtual int GetMaxYMomentum();
+
+  // get the underlying many-body state description for the canonical 
+  // representative in an orbit (used by Wannier construction)
+  //
+  // orbit = orbit index
+  // return value = unsigned long bit-pattern of the canonical Fock state
+  unsigned long GetStateDescription(int orbit);
+
+  // get the reordering sign for an orbit (sign convention used by 
+  // magnetic-translation orbit canonicalisation)
+  //
+  // orbit = orbit index
+  // return value = bit-packed reordering sign data for translations
+  unsigned long GetReorderingSign(int orbit);
+
+  // get the number of distinct momentum-translated states inside one orbit
+  //
+  // orbit = orbit index
+  // return value = number of states in the orbit (1, 2, ..., MaxMomentum)
+  int GetNbrStateInOrbit(int orbit);
   
   // return a list of all possible quantum numbers 
   //
@@ -503,6 +523,37 @@
 {
   return this->MaxMomentum;
 }
+
+// get the underlying many-body state description for the canonical
+// representative in an orbit
+//
+// orbit = orbit index
+// return value = unsigned long bit-pattern of the canonical Fock state
+
+inline unsigned long FermionOnTorusWithMagneticTranslations::GetStateDescription(int orbit)
+{
+  return this->StateDescription[orbit];
+}
+
+// get the reordering sign for an orbit
+//
+// orbit = orbit index
+// return value = bit-packed reordering sign data for translations
+
+inline unsigned long FermionOnTorusWithMagneticTranslations::GetReorderingSign(int orbit)
+{
+  return this->ReorderingSign[orbit];
+}
+
+// get the number of distinct momentum-translated states inside one orbit
+//
+// orbit = orbit index
+// return value = number of states in the orbit
+
+inline int FermionOnTorusWithMagneticTranslations::GetNbrStateInOrbit(int orbit)
+{
+  return this->NbrStateInOrbit[orbit];
+}
   
 // find canonical form of a state description
 //
